server:
  port: ${PORT:8080}

spring:
  application:
    name: hdb-backend
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:default}

logging:
  level:
    com.example.hdb: INFO
    org.springframework.web: INFO
