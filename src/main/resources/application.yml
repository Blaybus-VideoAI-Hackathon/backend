spring:
  application:
    name: hdb-backend
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:no-db}
    
server:
  port: ${PORT:8080}

logging:
  level:
    com.example.hdb: INFO
    org.springframework.web: INFO
